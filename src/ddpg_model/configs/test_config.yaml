#paths
paths:
  image_dir: datasets/test/images
  yolo_path: yolo_runs/test/finetune_exp1/weights/best.pt #path to finetuned weight of YOLO
  pt_path: ddpg_runs/ddpg_exp1/weights/ddpg_best.pt #path to weight of DDPG model

#agents
agent:
  state_dim: 516
  action_dim: 4
  max_step: 10

#save results
save:
  save_path: ddpg_runs/ddpg_exp1/inference
  save_figs: True
  